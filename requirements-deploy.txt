# ── WorkerSaathi — Production Dependencies ──────────────────────
# This file is used by the Dockerfile for cloud deployment.
# It excludes pyaudio (not needed on server, hard to install on Linux)
# and adds assemblyai SDK for LeMUR support.

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Voice (WebSocket proxy — no PyAudio needed on server)
websockets>=14

# RAG
huggingface-hub>=0.30.0
qdrant-client>=1.12.0
rank-bm25>=0.2.2
pymupdf>=1.28.0
ollama>=0.6.0

# AssemblyAI SDK (for LeMUR post-call analysis)
assemblyai>=0.37.0

# Web search (for search_live_news tool)
duckduckgo-search>=7.0.0
